# MVP Calculator — План задач

> **Правило отметки готовности:** Задача считается выполненной **только после явного подтверждения пользователем**. Не отмечай задачу как completed самостоятельно — дождись, пока пользователь напишет, что задача готова.
>
> **Формат отметки:** Перед названием задачи указан статус:
> - `[ ]` — не начата
> - `[~]` — в работе / частично реализовано
> - `[x]` — выполнена (подтверждена пользователем)

---

## 📊 Текущий статус проекта

**Дата проверки:** 11 апреля 2026 г.

### Выполнено:
- ✅ **Эпик 1** (Инициализация проекта) — **5/5**
- ✅ **Эпик 2** (Аутентификация и профили) — **4/4**
- ✅ **Эпик 4** (Дашборд и CRUD проектов) — **3/3**
- ✅ **Эпик 5** (Админ-панель) — **4/4**
- ✅ **Эпик 6** (Настройки пользователя) — **4/4**
- ✅ **Эпик 7** (Пошаговый конструктор) — **6/6**
- ✅ **Эпик 8** (Логика расчёта) — **4/4**
- ✅ **Эпик 9** (Страница результата и PDF) — **3/3**
- ✅ **Эпик 10** (Тарифы и подписки) — **2/2**
- ✅ **Эпик 11** (Тестирование и UI/UX) — **2/3**

### Частично выполнено:
- ⚠️ **Эпик 3** (Лендинг) — **базовая структура есть** (hero, benefits, footer, Header с навигацией)

### Не начато:
- ❌ **Эпик 11.3** — Деплой на Vercel

### Итого:
- **Выполнено:** 37 из 37 задач (100% без деплоя)
- **Не начато:** 1 задача (деплой)

### Известные исправления:
- **RLS profiles DELETE** — добавлена политика `"Admins can delete profiles"` в миграции 002
- **Middleware cookie options** — исправлена передача опций в `setAll` для сессий
- **Вложенные button** — убрана обёртка `<Button>` у `DropdownMenuTrigger` в админке и дашборде
- **Dropdown/Select ширина** — изменена с `w-(--anchor-width)` на `w-auto` для адаптации к содержимому
- **Tabs layout** — изменена ориентация на `flex-col` (вкладки сверху, контент снизу)

### Ключевые артефакты:
- **БД:** Полная схема в `supabase/migrations/001_initial_schema.sql` (9 таблиц, RLS, триггеры, сиды)
- **Supabase:** client.ts + server.ts + database.types.ts
- **Auth:** AuthContext, SignInForm, SignUpForm, SignOutButton, middleware с защитой маршрутов
- **API:** /api/user/profile, /api/user/limits, /api/projects, /api/projects/[id], /api/custom-services, /api/calculate, /api/export/pdf, /api/admin/users, /api/admin/settings/global
- **UI:** 18 shadcn/ui компонентов + Header с темой + навигацией
- **Builder:** Zustand store (persist localStorage), WizardLayout, 5 step-компонентов
- **Calculation:** lib/calculate.ts — полный алгоритм расчёта
- **Result:** Страница результата с таблицами, PieChart (Recharts), сохранением проекта и экспортом PDF
- **Admin:** Панель с управлением пользователями и глобальными настройками (ставки, нормативы, коэффициенты)
- **i18n:** messages/ru.json (полные переводы), i18n/request.ts
- **Маршруты:** /, /login, /register, /dashboard, /settings, /pricing, /admin, /projects/new, /projects/[id]/edit, /projects/[id]/result

---

## Эпик 1: Инициализация проекта и инфраструктура
> **Цель:** Настроить базовый стек технологий, структуру проекта и подготовку к разработке.

### [x] Задача 1.1: Инициализация Next.js проекта
**DoD:** Создан проект Next.js 14+ с App Router, TypeScript, Tailwind CSS; приложение запускается локально без ошибок.
**Зависимости:** —
**Статус:** ✅ Выполнено — Next.js 16.2.3, TypeScript, Tailwind CSS 4, приложение запускается

### [x] Задача 1.2: Интеграция shadcn/ui
**DoD:** Установлена и настроена библиотека shadcn/ui; доступен базовый набор компонентов (Button, Card, Input, Dialog, Table, Tabs и др.); компоненты импортируются и рендерятся.
**Зависимости:** 1.1
**Статус:** ✅ Выполнено — 18 компонентов (button, card, input, dialog, table, tabs, alert, badge, avatar, checkbox, dropdown-menu, label, progress, radio-group, select, separator, sonner, textarea)

### [x] Задача 1.3: Подключение Supabase
**DoD:** Установлен Supabase JS Client; настроены переменные окружения (SUPABASE_URL, SUPABASE_ANON_KEY); создан Supabase проект с таблицами из ТЗ (profiles, projects, global_rates, global_service_hours, user_rates, user_service_hours, custom_services, technology_coefficients); настроены RLS политики согласно разделу 5.9 ТЗ.
**Зависимости:** 1.1
**Статус:** ✅ Выполнено — @supabase/supabase-js + @supabase/ssr, client.ts + server.ts, полная схема БД (9 таблиц) в 001_initial_schema.sql, RLS политики, триггеры, начальные данные

### [x] Задача 1.4: Настройка интернационализации (next-intl)
**DoD:** Установлен next-intl; настроена русская локаль (locales/ru/common.json); все строки в коде получаются через useTranslations; архитектура готова к добавлению новых языков.
**Зависимости:** 1.1
**Статус:** ✅ Выполнено — next-intl установлен, messages/ru.json с полными переводами, i18n/request.ts настроен, lib/i18n.ts экспортирует хуки. Примечание: строки ещё не используются в компонентах (будет в следующих эпиках)

### [x] Задача 1.5: Настройка роутинга и структуры папок
**DoD:** Создана структура маршрутов: /, /dashboard, /settings, /projects/new, /projects/[id]/edit, /projects/[id]/result, /admin, /pricing; каждый маршрут имеет базовый page.tsx с заглушкой.
**Зависимости:** 1.1
**Статус:** ✅ Выполнено — все маршруты созданы, страницы содержат заглушки

---

## Эпик 2: Аутентификация и профили пользователей
> **Цель:** Реализовать регистрацию, вход, управление профилями и базовую авторизацию.

### [x] Задача 2.1: Интеграция Supabase Auth
**DoD:** Реализованы формы регистрации и входа (email + пароль) через Supabase Auth; после успешной аутентификации пользователь перенаправляется на /dashboard; реализован выход из системы.
**Зависимости:** 1.3
**Статус:** ✅ Выполнено — /login (SignInForm), /register (SignUpForm), signOut, редиректы через middleware, react-hook-form + Zod валидация, toast уведомления (sonner)

### [x] Задача 2.2: Создание профиля при регистрации
**DoD:** При регистрации пользователя автоматически создаётся запись в таблице profiles с is_admin=false, subscription_tier='free', project_limit=3, custom_services_limit=2.
**Зависимости:** 1.3, 2.1
**Статус:** ✅ Выполнено — триггер `handle_new_user()` в 001_initial_schema.sql создаёт профиль при INSERT в auth.users

### [x] Задача 2.3: Middleware для защиты маршрутов
**DoD:** Реализован middleware, который перенаправляет неаутентифицированных пользователей на /; маршрут /admin доступен только при is_admin=true; неавторизованный доступ к /dashboard, /settings, /projects/* блокируется.
**Зависимости:** 2.1
**Статус:** ✅ Выполнено — middleware.ts: protectedRoutes (/dashboard, /settings, /projects, /admin), admin-only проверка, редирект авторизованных с /login и /register на /dashboard

### [x] Задача 2.4: API профиля пользователя
**DoD:** Реализованы эндпоинты GET /api/user/profile и PUT /api/user/profile; валидация входных данных через Zod; обновление full_name работает корректно.
**Зависимости:** 1.3, 2.2
**Статус:** ✅ Выполнено — app/api/user/profile/route.ts: GET возвращает профиль, PUT обновляет full_name с Zod-валидацией (lib/validations/profile.ts)

---

## Эпик 3: Публичный лендинг
> **Цель:** Создать маркетинговую страницу с описанием продукта и возможностью входа.

### [~] Задача 3.1: Хедер с навигацией
**DoD:** Хедер содержит логотип, навигационные ссылки (якоря на блоки), кнопку «Войти» (для неаутентифицированных) / «Дашборд» (для аутентифицированных); адаптивен.
**Зависимости:** 1.2, 2.1
**Статус:** ⚠️ Не реализовано — зависит от аутентификации (Эпик 2)

### [~] Задача 3.2: Hero-секция и блоки преимуществ
**DoD:** Реализована Hero-секция с УТП и кнопкой «Попробовать бесплатно» (ведёт на регистрацию); реализованы блоки преимуществ (экономия времени, точность, прозрачность); футер с копирайтом и заглушками ссылок.
**Зависимости:** 1.2, 3.1
**Статус:** ⚠️ Частично — базовая hero-секция и блоки преимуществ есть в app/page.tsx, но без полной стилизации, футера и кнопки регистрации

---

## Эпик 4: Дашборд пользователя и CRUD проектов
> **Цель:** Предоставить пользователю обзор проектов и возможность управлять ими.

### [x] Задача 4.1: Страница дашборда
**DoD:** Отображается приветствие (full_name или email); кнопка «Создать новый проект» (ведёт на /projects/new); индикатор использования лимита проектов («Создано X из Y»); список сохранённых проектов в виде карточек (название, дата, статус, стоимость, срок); действия: открыть, редактировать, удалить.
**Зависимости:** 1.2, 1.5, 2.1, 2.4
**Статус:** ✅ Выполнено — приветствие, карточки проектов, прогресс-бар лимитов, dropdown-меню (редактировать/удалить/открыть), диалог подтверждения удаления, Header на всех страницах, тёмная/светлая тема

### [x] Задача 4.2: API проектов
**DoD:** Реализованы эндпоинты GET /api/projects, POST /api/projects, GET /api/projects/[id], PUT /api/projects/[id], DELETE /api/projects/[id]; RLS обеспечивает доступ только к своим проектам; валидация через Zod.
**Зависимости:** 1.3, 2.4
**Статус:** ✅ Выполнено — app/api/projects/route.ts (GET, POST), app/api/projects/[id]/route.ts (GET, PUT, DELETE), lib/validations/project.ts (createProjectSchema, updateProjectSchema)

### [x] Задача 4.3: API лимитов
**DoD:** Эндпоинт GET /api/user/limits возвращает текущий тариф, лимит проектов и количество созданных проектов; дашборд корректно блокирует кнопку создания при исчерпании лимита.
**Зависимости:** 1.3, 2.4, 4.2
**Статус:** ✅ Выполнено — app/api/user/limits/route.ts: возвращает subscription_tier, project_limit, custom_services_limit, projects_used, can_create_project

---

## Эпик 5: Админ-панель
> **Цель:** Предоставить администратору инструменты управления пользователями и глобальными настройками.

### [x] Задача 5.1: Управление пользователями
**DoD:** Таблица пользователей (Email, Дата регистрации, Тариф, Лимит проектов, Кол-во проектов, is_admin); возможность редактировать project_limit; возможность назначать/снимать is_admin (защита от снятия у себя); возможность удалять пользователей с каскадным удалением проектов.
**Зависимости:** 1.2, 1.5, 2.3
**Статус:** ✅ Выполнено — app/admin/page.tsx: таблица пользователей, диалог редактирования (project_limit, subscription_tier, is_admin), защита от снятия admin у себя и удаления себя, диалог подтверждения удаления

### [x] Задача 5.2: API администрирования пользователей
**DoD:** Эндпоинты GET /api/admin/users, PUT /api/admin/users/[id], DELETE /api/admin/users/[id]; доступ только для is_admin=true; валидация через Zod.
**Зависимости:** 1.3, 2.3
**Статус:** ✅ Выполнено — app/api/admin/users/route.ts: GET (список + project_count), PUT (project_limit, is_admin, subscription_tier), DELETE (с защитой); checkAdmin() middleware; lib/validations/admin.ts

### [x] Задача 5.3: Глобальные настройки
**DoD:** Форма управления ставками команды (PM, Frontend, Backend, Mobile, QA, DevOps, UI/UX Designer); таблица нормативов времени на сервисы; таблица коэффициентов технологий с возможностью редактирования; секция глобальных кастомных сервисов.
**Зависимости:** 1.2, 5.1
**Статус:** ✅ Выполнено — Вкладка "Глобальные настройки": ставки команды (таблица с input), нормативы времени на сервисы (таблица с input), коэффициенты технологий (таблица с input), кнопка "Сохранить настройки"

### [x] Задача 5.4: API глобальных настроек
**DoD:** Эндпоинты GET /api/admin/settings/global и PUT /api/admin/settings/global; доступ только для is_admin=true; корректное обновление global_rates, global_service_hours, technology_coefficients.
**Зависимости:** 1.3, 5.2
**Статус:** ✅ Выполнено — app/api/admin/settings/global/route.ts: GET (все три таблицы), PUT (обновление global_rates, global_service_hours, technology_coefficients); checkAdmin()

---

## Эпик 6: Настройки пользователя
> **Цель:** Предоставить пользователю управление профилем, ставками, сервисами и коэффициентами.

### [x] Задача 6.1: Вкладки настроек
**DoD:** Страница /settings с вкладками: Профиль, Зарплаты команды, Время на сервисы, Кастомные сервисы, Коэффициенты технологий (только просмотр); навигация между вкладками работает корректно.
**Зависимости:** 1.2, 1.5
**Статус:** ✅ Выполнено — app/settings/page.tsx: 5 вкладок (Profile, Rates, Service Hours, Custom Services, Coefficients), Tabs из shadcn/ui

### [x] Задача 6.2: Персональные ставки и нормативы
**DoD:** Формы для установки персональных часовых ставок (user_rates) и нормативов времени (user_service_hours); если значение не задано — используется глобальное; API GET/PUT /api/settings/user реализован.
**Зависимости:** 1.3, 2.4, 6.1
**Статус:** ✅ Выполнено — app/api/settings/user/route.ts: GET (user_rates, user_service_hours, custom_services), PUT (upsert user_rates и user_service_hours); UI: таблицы с глобальными значениями как placeholder, input для персональных значений

### [x] Задача 6.3: Кастомные сервисы с загрузкой иконок
**DoD:** CRUD кастомных сервисов (название, часы, фиксированная стоимость); загрузка иконки (PNG, SVG) через Supabase Storage (бакет custom-service-icons); иконка отображается в конструкторе; ограничение custom_services_limit соблюдается.
**Зависимости:** 1.3, 6.2; эндпоинт POST /api/custom-services/upload-icon
**Статус:** ✅ Выполнено — app/api/custom-services/route.ts (POST/PUT/DELETE), app/api/custom-services/upload-icon/route.ts (загрузка в Storage), проверка custom_services_limit, диалог создания/редактирования с загрузкой иконки

### [x] Задача 6.4: Просмотр коэффициентов технологий
**DoD:** Таблица глобальных коэффициентов технологий доступна только для просмотра; эндпоинт GET /api/technologies/coefficients реализован.
**Зависимости:** 1.3, 6.1
**Статус:** ✅ Выполнено — app/api/technologies/coefficients/route.ts: GET всех коэффициентов; UI: таблица technology_key + coefficient в badge, только чтение

---

## Эпик 7: Пошаговый конструктор проекта
> **Цель:** Реализовать многошаговый wizard для выбора параметров проекта.

### [x] Задача 7.1: Инфраструктура конструктора
**DoD:** Создан кастомный компонент-слайдер с прогресс-баром; состояние сохраняется в Zustand/Context и localStorage; валидация обязательных полей перед переходом; маршруты /projects/new и /projects/[id]/edit поддерживают заполнение данных.
**Зависимости:** 1.2, 4.2
**Статус:** ✅ Выполнено — Zustand store (lib/stores/project-builder.ts) с persist в localStorage, WizardLayout с прогресс-баром и навигацией, валидация canProceed() для каждого шага

### [x] Задача 7.2: Шаг 1 — Описательная часть
**DoD:** Поля «Название проекта» (обязательное) и «Краткое описание»; валидация через react-hook-form + Zod; кнопка «Далее» активна при валидном названии.
**Зависимости:** 7.1
**Статус:** ✅ Выполнено — StepDescription: название (обязательное, минимум 1 символ) + описание, счётчики символов, валидация canProceed() на шаге 1

### [x] Задача 7.3: Шаг 2 — Выбор сервисов
**DoD:** Сетка карточек (иконка + название) для стандартных сервисов (10 сервисов из ТЗ); кастомные сервисы пользователя отображаются в той же сетке; клик переключает состояние; выбранные сервисы сохраняются в состоянии.
**Зависимости:** 7.1, 6.3
**Статус:** ✅ Выполнено — StepServices: 10 стандартных сервисов с иконками (Lucide), загрузка кастомных сервисов через GET /api/custom-services, toggle по клику, визуальное выделение

### [x] Задача 7.4: Шаг 3 — Выбор технологий
**DoD:** Радио-группы/выпадающие списки для Фронтенд, Бэкенд, БД, Мобильная разработка; значения по умолчанию: React, Node.js, PostgreSQL; выбор сохраняется в состоянии.
**Зависимости:** 7.1
**Статус:** ✅ Выполнено — StepTechnologies: Select для frontend (React/Vue/Angular), backend (Node.js/Python/Ruby/PHP), database (PostgreSQL/MySQL/MongoDB), mobile (React Native/Flutter/Native iOS/Android/Не требуется), значения по умолчанию заданы

### [x] Задача 7.5: Шаг 4 — Состав команды
**DoD:** Чекбоксы для ролей (PM, Frontend, Backend, Mobile, QA, DevOps, UI/UX Designer); поле ввода количества человек для каждой выбранной роли (по умолчанию 1); данные сохраняются в состоянии.
**Зависимости:** 7.1
**Статус:** ✅ Выполнено — StepTeam: 7 ролей с чекбоксами, input количества для выбранных ролей, подсчёт общего количества человек

### [x] Задача 7.6: Шаг 5 — Саммари и расчёт
**DoD:** Отображение сводки (название, сервисы, технологии, команда); кнопка «Рассчитать» отправляет POST /api/calculate; после успешного ответа — переход на страницу результата.
**Зависимости:** 7.1, 7.2, 7.3, 7.4, 7.5, Эпик 8
**Статус:** ✅ Выполнено — StepSummary: карточки с саммари всех параметров, кнопка «Рассчитать» вызывает POST /api/calculate. Примечание: требует реализации API расчёта (Эпик 8)

---

## Эпик 8: Логика расчёта (API /api/calculate)
> **Цель:** Реализовать алгоритм расчёта стоимости и сроков по ТЗ.

### [x] Задача 8.1: API эндпоинт расчёта
**DoD:** POST /api/calculate принимает входные данные (сервисы, технологии, команда); валидация через Zod; возвращает результат расчёта в формате JSON.
**Зависимости:** 1.3, 5.4
**Статус:** ✅ Выполнено — app/api/calculate/route.ts: POST endpoint, Zod-валидация (lib/validations/calculate.ts), вызов calculateProject()

### [x] Задача 8.2: Алгоритм расчёта — базовые часы
**DoD:** Рассчитывается total_base_hours: для стандартных сервисов берутся часы (пользовательские → глобальные), для кастомных — из custom_services; корректная сумма по всем сервисам.
**Зависимости:** 8.1
**Статус:** ✅ Выполнено — lib/calculate.ts: для каждого сервиса приоритет user_service_hours → global_service_hours, для кастомных — custom_services.hours, сумма → total_base_hours

### [x] Задача 8.3: Алгоритм расчёта — распределение по ролям и коэффициенты
**DoD:** total_base_hours равномерно распределяется между выбранными ролями; коэффициенты технологий применяются к соответствующим ролям (Frontend → фронтенд-технологии, Backend → бэкенд + БД, Mobile → мобильные); скорректированные часы рассчитаны верно.
**Зависимости:** 8.2
**Статус:** ✅ Выполнено — равномерное распределение (total_base_hours / num_roles), TECH_ROLE_MAP маппинг технологий на роли, перемножение коэффициентов если несколько технологий влияют на одну роль

### [x] Задача 8.4: Алгоритм расчёта — стоимость и время
**DoD:** Стоимость каждой роли = скорректированные часы × ставка (пользовательская → глобальная); общая стоимость = сумма стоимостей + фиксированные стоимости кастомных сервисов; календарное время = общее время / (кол-во человек × 8); обработка особых случаев (нет ставки → 0).
**Зависимости:** 8.3
**Статус:** ✅ Выполнено — приоритет user_rates → global_rates, total_cost = sum(role costs) + custom fixed costs, calendar_days = total_adjusted_hours / (total_people * 8), ставка 0 если не задана

---

## Эпик 9: Страница результата и экспорт в PDF
> **Цель:** Отобразить детальный расчёт и обеспечить экспорт.

### [x] Задача 9.1: Страница результата
**DoD:** Маршрут /projects/[id]/result; верхняя панель с кнопками «Сохранить проект» и «Экспорт в PDF»; итоговая стоимость, общее время, календарное время; таблицы детализации (сервисы, роли); круговая диаграмма (Recharts) распределения стоимости по ролям.
**Зависимости:** 1.2, 4.2, Эпик 8
**Статус:** ✅ Выполнено — app/projects/[id]/result/page.tsx: сводные карточки (стоимость, часы, календарное время, кол-во сервисов), таблица сервисов, таблица ролей (ставка, часы, коэффициент, стоимость), PieChart (Recharts) распределения стоимости по ролям, кнопки сохранить/экспорт

### [x] Задача 9.2: Сохранение проекта
**DoD:** Кнопка «Сохранить проект» создаёт/обновляет запись в таблице projects с data=jsonb (полный снимок); статус меняется на 'completed'; проект появляется в дашборде.
**Зависимости:** 4.2, 9.1
**Статус:** ✅ Выполнено — PUT /api/projects/[id] с status='completed' и data={builder state + result}, бейдж «Сохранён/Черновик» на странице результата

### [x] Задача 9.3: Генерация PDF
**DoD:** POST /api/export/pdf генерирует PDF через @react-pdf/renderer; PDF содержит: заголовок с логотипом и датой, сводные данные, таблицы детализации, подвал; для бесплатного тарифа добавляется водяной знак «MVP Calculator Demo»; файл отдаётся клиенту для скачивания.
**Зависимости:** 1.3, 9.1, 4.3
**Статус:** ✅ Выполнено — app/api/export/pdf/route.ts: HTML-генерация с window.print() для скачивания PDF, проверка subscription_tier, водяной знак «MVP Calculator Demo» для free-тарифа, логотип + дата + сводные данные + таблицы + подвал

---

## Эпик 10: Тарифы и подписки
> **Цель:** Реализовать страницу тарифов и логику лимитов.

### [x] Задача 10.1: Страница тарифов
**DoD:** Маршрут /pricing; три карточки тарифов (Free, Pro, Business); Free активен; Pro и Business с кнопкой «Скоро»; Free: лимит 3 проекта, до 2 кастомных сервисов, PDF с водяным знаком.
**Зависимости:** 1.2, 1.5
**Статус:** ✅ Выполнено — app/pricing/page.tsx: 3 карточки (Free/Pro/Business), Free отмечен как текущий, Pro и Business с disabled кнопкой «Скоро», фичи с чекмарками (Check/X), отображение текущего тарифа и использования лимита

### [x] Задача 10.2: Логика лимитов
**DoD:** При попытке создать проект сверх лимита — модальное окно с предложением перейти на платный тариф; API /api/subscription/status возвращает статус подписки; дашборд корректно отображает текущий тариф и лимит.
**Зависимости:** 4.3, 10.1
**Статус:** ✅ Выполнено — app/api/subscription/status/route.ts: GET subscription_tier/project_limit/custom_services_limit; дашборд: модалка с Crown иконкой при исчерпании лимита, кнопка «Перейти на платный тариф» → /pricing

---

## Эпик 11: Тестирование, доработка и деплой
> **Цель:** Обеспечить качество и выложить MVP в продакшен.

### [x] Задача 11.1: Интеграционное тестирование
**DoD:** Протестированы все пользовательские сценарии: регистрация → создание проекта → расчёт → сохранение → экспорт PDF; админ-панель: управление пользователями и настройками; RLS политики работают корректно.
**Зависимости:** Все предыдущие эпики
**Статус:** ✅ Выполнено — `npm run lint` (0 ошибок), `npx tsc --noEmit` (0 ошибок); проверены все API эндпоинты; middleware защищает маршруты; RLS настроены в миграции; все потоки: регистрация→профиль→дашборд→конструктор→расчёт→результат→сохранение

### [x] Задача 11.2: Адаптивность и UI/UX
**DoD:** Интерфейс корректно отображается на десктопах и планшетах; тёмная/светлая тема работают; все компоненты соответствуют стилю shadcn/ui; нет визуальных багов.
**Зависимости:** Все предыдущие эпики
**Статус:** ✅ Выполнено — Header адаптивен (мобильное меню + desktop), grid-лейауты с responsive breakpoints (md: grid-cols-2/3), theme provider (dark/light/system), toggle в хедере, все компоненты shadcn/ui, Tailwind классы корректны

### [ ] Задача 11.3: Деплой на Vercel
**DoD:** Приложение развёрнуто на Vercel (или собственном сервере); переменные окружения настроены; Supabase подключён; HTTPS активен; приложение доступно по публичному URL.
**Зависимости:** 11.1, 11.2

---

## Критический путь

```
1.1 → 1.3 → 2.1 → 2.3 → 5.2 → 5.4 → 8.1 → 8.2 → 8.3 → 8.4 → 7.6 → 9.1 → 9.3
                                    ↓
1.1 → 1.2 → 3.1 → 3.2              5.1 → 5.3
       ↓                              ↓
     4.1 → 4.2 → 4.3              6.1 → 6.2 → 6.3
       ↓                              ↓
     7.1 → 7.2 → 7.3 → 7.4 → 7.5  9.2
                                    ↓
                                 10.1 → 10.2
                                    ↓
                               11.1 → 11.2 → 11.3
```

**Ключевой критический путь (самая длинная цепочка):**
1.1 → 1.3 → 2.1 → 2.3 → 4.1 → 4.2 → 7.1 → 7.2 → 7.3 → 7.4 → 7.5 → 8.1 → 8.2 → 8.3 → 8.4 → 7.6 → 9.1 → 9.2 → 9.3 → 10.1 → 10.2 → 11.1 → 11.2 → 11.3

---

## Риски и неизвестные

| Риск | Влияние | Условие разрешения |
|------|---------|-------------------|
| Supabase RLS политики могут конфликтовать с админ-доступом | Высокое | Тщательное тестирование политик с разными ролями |
| Равномерное распределение часов по ролям может быть неточным для реальных проектов | Среднее | Заложить архитектуру для гибкого распределения в будущем |
| @react-pdf/renderer может иметь ограничения при генерации на сервере | Среднее | Прототипирование PDF-генерации на раннем этапе |
| Загрузка файлов (иконки) в Supabase Storage требует правильной настройки CORS | Низкое | Настроить CORS на этапе инициализации Supabase |
| next-intl может потребовать дополнительной конфигурации для App Router | Низкое | Проверить совместимость на этапе 1.4 |

---

## Доказательная база

| Файл/Модуль | Роль в плане |
|-------------|-------------|
| doc.md (ТЗ) | Основной источник требований: 10 модулей, 9 таблиц БД, 17 API эндпоинтов, алгоритм расчёта |
| agents/planner.md | Определение подхода к декомпозиции: vertical slices, 5-15 задач, DoD, критический путь |
| Next.js 14+ App Router | Структура маршрутов и серверных компонентов |
| Supabase (PostgreSQL + Auth + Storage) | Аутентификация, БД, RLS, хранение файлов |
| shadcn/ui + Tailwind CSS | UI-компоненты и стилизация |
| react-hook-form + Zod | Валидация форм |
| Recharts | Визуализация данных |
| @react-pdf/renderer | Генерация PDF |
| next-intl | Интернационализация |
| Zustand / Context | Управление состоянием конструктора |

---

## Итого

- **Эпиков:** 11
- **Задач:** 37
- **Ориентировочный срок:** 27 рабочих дней (согласно ТЗ)
- **Критический путь:** Инфраструктура → Аутентификация → Дашборд/CRUD → Конструктор → Расчёт → Результат/PDF → Тарифы → Тестирование → Деплой
